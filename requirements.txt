# Django và các package cơ bản
Django>=4.2.10
django-allauth>=0.57.0
django-jazzmin>=2.6.0
channels>=4.0.0
django-widget-tweaks>=1.5.0
django-humanize>=0.1.2

# Database
psycopg2-binary>=2.9.0

# GeoDjango - COMMENTED OUT for Render deployment (requires system dependencies)
# GDAL>=3.4.0

# RAG / Embeddings - COMMENTED OUT for free tier (too large)
# numpy<2
# sentence-transformers>=2.2.2
# torch>=2.2.0

# Groq client (lightweight)
groq>=0.5.0

# Form/field helpers
django-multiselectfield>=0.1.12
PyJWT>=2.8.0
python-dateutil>=2.9.0
pytz>=2024.1

# Production server
gunicorn>=21.2.0
whitenoise>=6.6.0
dj-database-url>=2.1.0


